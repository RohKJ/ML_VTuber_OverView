# VTuber View Forecast - Python 의존성 (코랩/로컬 공용)
#
# 설치:  pip install -r requirements.txt
#
# [따로 챙겨야 하는 2가지]  - 자세한 건 파일 맨 아래 참고
#   1) torch (파이토치): 이 목록에 없음. 코랩은 이미 설치됨, 로컬은 별도 설치.
#   2) ffmpeg          : pip 패키지가 아님(프로그램). 코랩은 apt, 로컬은 따로 설치.
#
# 코랩은 아래 상당수가 기본 설치돼 있어 재설치가 생략될 수 있다(무해).

# ---- 공통 (전 단계) ----
numpy
pandas
scipy
python-dotenv
tqdm
openpyxl            # .xlsx 읽기/쓰기

# ---- 시각화 (EDA / Modeling) ----
matplotlib
seaborn

# ---- 00 데이터수집: YouTube Data API ----
google-api-python-client
google-auth-httplib2
google-auth-oauthlib
yt-dlp              # 영상 -> wav 추출 (ffmpeg 필요)

# ---- 00 데이터수집: Gemini ----
google-genai

# ---- 00 데이터수집: 썸네일 (CV / OCR) ----
requests
opencv-python-headless   # GUI 없는 빌드. 코랩/로컬 양쪽에서 동작
Pillow
easyocr
gdown

# ---- 00 데이터수집: CLIP 스코어링 ----
# 참고: CLIP 노트북은 코랩에서 Pillow==11.2.1, transformers==4.48.0 으로
#       고정 설치 후 런타임 재시작을 한다(노트북 셀 참고). 여기선 핀 없이 둔다.
transformers

# ---- 00 데이터수집: 음성 피처 (GPU 권장, 사실상 코랩 전용) ----
soundfile
librosa
praat-parselmouth   # import 이름: parselmouth
pyloudnorm
webrtcvad
opensmile
demucs              # 보컬 분리 (torch/torchaudio 필요)
faster-whisper      # 발화 구간/STT

# ---- 04 모델링 ----
scikit-learn
statsmodels
shap
xgboost
lightgbm
catboost
optuna
plotly              # optuna.visualization 용
kaleido             # optuna 그래프 정적 이미지 저장(선택)

# =====================================================================
# [별도 설치 안내] - 위 목록에 일부러 넣지 않은 것들
# =====================================================================
#
# 1) torch / torchaudio (demucs, faster-whisper, CLIP가 사용)
#    - 코랩: GPU 런타임에 이미 설치돼 있으므로 따로 설치하지 말 것.
#    - 로컬: 환경에 맞는 빌드를 직접 설치.
#        CPU만:   pip install torch torchaudio
#        NVIDIA GPU: https://pytorch.org/get-started/locally/ 안내대로 설치
#
# 2) ffmpeg (yt-dlp의 wav 추출에 필요. pip 패키지가 아님)
#    - 코랩/리눅스: apt-get install -y ffmpeg
#    - Windows:    winget install Gyan.FFmpeg  (또는 ffmpeg.org에서 받아 PATH 등록)
